#include<bits/stdc++.h>
using namespace std;
#define ll long long

void search(ll n){
    ll c = 0;
    for(ll x = 5; n / x >= 1; x *= 5)
        c += n / x;
    cout<<c;
}
int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    ll n;
    cin>>n;
    search(n);
    return 0;
}
